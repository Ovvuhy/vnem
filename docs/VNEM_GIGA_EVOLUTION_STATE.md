# VNEM GIGA Evolution State

- Current phase: Phase 8 terminal and project automation implementation, real MCP proof, execution benchmark, affected validation, registry generation, readiness checks, checkpoint commit, and push are complete. Fresh Actions run `29259837515` exposed a branch-caused POSIX dev-server process-tree cleanup bug; the smallest runtime/test fix and regenerated benchmark pass locally. Fix commit/push and fresh green Actions proof remain. Phase 2 domain extraction remains in progress.
- Completed phase commits: Phase 0 PR #14 merge `b585a0e`; Phase 1 baseline `4488a3e`; Phase 2 runtime contracts `857e5b8`; draft-PR state `44afd5e`; thin public launchers `adcba8c`; Phase 3 Precision integration `ca95e67`; Phase 3 route preservation fix `24e81d2`; Phase 4 scoped safety profiles and controls `3213733`; Phase 5 client/safety setup `3fe29bf`; Phase 6 adaptive Core intelligence `f38444e`; Phase 7 verified app engineering `90051d4`; Phase 7 Node 20 browser transport `156cf87`; Phase 7 Linux Chromium startup hardening `9b02e50`; Phase 8 resilient project automation `7dce21a`. The POSIX process-group cleanup fix is in the commit containing this state.
- Branch checkpoint context: `feat/vnem-giga-evolution-1`; local HEAD, remote branch, and draft PR head were all verified at Phase 8 checkpoint `7dce21afc1c42c4ae92c8e392be7e06f43da9649` before the POSIX cleanup fix.
- Phase 5 architecture: `scripts/vnem/clients` now owns the client truth catalog, platform path resolution, JSON/TOML merge, client detection, preview/apply transactions, private backups, atomic writes, exact rollback, proof, MCP smoke, interactive selection, and CLI routing. Existing project-pack and adoption commands remain compatible.
- Commands implemented: `vnem setup`, `vnem clients`, `vnem safety`, `vnem doctor`, `vnem rollback`, `vnem benchmark`, `vnem status`, and `vnem config preview`; all have noninteractive paths and setup mutations require one explicit confirmation or `--yes`.
- Client support truth: Codex App/CLI direct TOML merge; Claude Code project JSON; Claude Desktop, Cursor, and Gemini CLI direct documented JSON; generic stdio import profile; Antigravity, Hermes, Windsurf, and Cline import-only where a current global config contract is not verified. Windows/Linux/macOS catalog fixtures cover 11 named client surfaces.
- Config safety proof: Codex App and CLI deduplicate to one `config.toml` transaction; unrelated TOML/JSON settings and unknown VNEM server/env keys are preserved; malformed markers, invalid syntax, and changed-after-preview hashes block apply; `@iarna/toml` validates full TOML without a Python dependency; generated import profiles are ignored by Git.
- Real Codex proof: local config backup/merge transaction `setup-2026-07-12T14-17-59-532Z-b7be2ad2` was rolled back byte-for-byte to SHA `00ae9d8efcfd188325fe2637ce871b60e44ce7d6d8659ef7a8eefbb735c1eb64`, including removal of the newly created safety file. Intended final state was restored by green transaction `setup-2026-07-12T14-19-03-468Z-9f2a3fcb`; final Codex config SHA is `6aeb93f5c3a583943de6a34192c0b6ecf77f7a7b7add6ee3e57b9b99a42bab87`; active safety profile is `safe-local-dev` with hard blocks present.
- Real MCP proof through Phase 6: Core listed 68 tools and called `vnem_entrypoint`; Tools listed 150 tools before Phase 7 and called `vnem_tools_entrypoint`; latest doctor passed with both configs present and TOML syntax valid. Codex App UI restart was intentionally not performed while this task is active. The installed WindowsApps `codex.exe` is detected but `codex mcp list` remains OS-blocked with Access denied even outside the sandbox, so post-restart client-UI visibility is not yet proven.
- Phase 6 architecture: `scripts/vnem/core/intelligence.mjs` is now the single decision implementation used by `vnem_entrypoint`, the Core visibility doctor, and the underuse detector. The old monolithic entrypoint/classifier block was removed. Core now registers 68 live tools; the combined runtime registry contains 223 tools and 205 have behavior-test references.
- Phase 6 decision behavior: weighted adapters use goal, explicit task mode, compact context, repo signals, user constraints, and compatibility facts. Mixed app/UI, package/CI, API/security, Windows/project, game/modding, skill, data, client, research, recovery, remote, debugging, and proof domains remain visible. The route is capped at six exact registered Tools calls and balances material domains before deeper optional steps.
- Phase 6 compact/follow-up behavior: default output includes material missing context, safe assumptions, quality domains, capability effects, exact Tools state (`available`, `configured`, `allowed`, `executed`, `succeeded`, `proof_collected`), permission implications, compatibility constraints, evidence requirements, stop conditions, completion criteria, and one next action. `vnem_decision_details` retrieves session-scoped deeper sections by deterministic id.
- Phase 6 compatibility/evidence behavior: `vnem_compatibility_assess` keeps OS, shell, runtime, package manager, framework, project type, client, MCP transport, game/mod loader, file format, API auth/provider, GitHub permission, and browser facts task-scoped with evidence and unknown boundaries. `vnem_continue_from_tools_evidence` returns complete/incomplete/blocked, failed reruns, claim overreach, remaining requirements, user-input need, and the smallest next action without claiming Core executed Tools.
- Tests passing: uninterrupted full Phase 6 `npm test`; focused MCP intelligence; both adoption generations; Core readiness with every GIGA marker yes; Core tool selection/routing/adaptive/compact/output/permission/research/debug/UI suites; MCP smoke; Core-Tools E2E/ecosystem; user smoke; GIGA baseline harness; runtime/behavior/artifact checks; and registry validation. The full Windows run crossed the post-omniscient boundary and ended naturally with exit code 0.
- Benchmark baseline location: `.vnem/giga-evolution/baseline`.
- Benchmark current location: `.vnem/giga-evolution/phase-6`; 35 scenarios, 26 strict successes versus Phase 5's 19, mean score `0.9640` versus `0.9024`, seven new successes, zero scenario regressions, median combined route latency `10.91 ms` versus `5.20 ms`, and median compact text output `733` versus `716` bytes.
- Phase 6 performance proof: second cold-process sample recorded Core startup `940.44 ms`, Tools `771.96 ms`, and Precision `682.31 ms`; all three were uniformly slower than Phase 5 on the host, so startup regression is not attributed to Core without cleaner environment proof. Core entrypoint median was `20.94 ms` versus `7.12 ms`, while compact text shrank to `350` from `450` bytes. Generation remained deterministic. Full-suite status is `pass`; wall time was not captured, and the existing Vite large-chunk warning remained non-fatal.
- Phase 7 architecture: `scripts/vnem/tools/app-engineering.mjs` owns bounded framework/project inspection, frontend/backend/shared-boundary and route/component/API/data-flow detection, completion-state and quality-signal inspection, deterministic adapter templates, hash-bound plans, staged multi-file transactions, rollback manifests, and a dedicated-profile Chromium DevTools Protocol user-path runner. Tools now exposes five cohesive calls: `vnem_tools_app_inspect`, `vnem_tools_app_vertical_slice_plan`, `vnem_tools_app_vertical_slice_apply`, `vnem_tools_app_acceptance_run`, and `vnem_tools_app_transaction_rollback`.
- Phase 7 support truth: marker-backed `vite-react-node` and `static-node` adapters have verified generation and execution. Next-style projects are inspection/plan-only and generic projects are inspection-only until a reviewed adapter is added. Automatic mutation refuses unmarked projects. The transaction uses content-hash preconditions, sibling staging, per-file rename commits, retained originals, and automatic all-or-rollback semantics; it explicitly does not claim impossible cross-file filesystem atomicity.
- Phase 7 vertical slice: both adapters generate a visible task workflow with GET/POST API handling, shared domain state, validation, loading/empty/error/success states, labels/status/alert/focus accessibility signals, responsive CSS, focused domain tests, build scripts, and localhost dev scripts. The acceptance tool runs allowlisted test/build scripts, starts and verifies localhost, exercises a real form submission, records console and network events, captures desktop/mobile screenshots, stops the actual Windows listener PID, and optionally restores the transaction on failed acceptance.
- Phase 7 real MCP proof: `scripts/test-tools-giga-app-engineering.mjs` uses stdio MCP against copied representative fixtures. It proves tool registration, incomplete-boundary detection, honest unsupported/unmarked blocking, dry-run and explicit approval, real 10-file apply for each adapter, completed architecture/state signals, focused test/build success, real Edge Chromium desktop interaction, mobile no-overflow render, API 200/201 responses, zero console errors, zero network failures/bad responses, screenshot files, failed-acceptance automatic restore, explicit rollback, and zero remaining test listeners/processes. The pre-existing project-action test also passes with the listener cleanup hardening.
- Phase 7 execution benchmark: `.vnem/giga-evolution/phase-7/execution-benchmark.json` is an actual stdio MCP project execution run, not a route score. Latest sample completed both adapters in `19.26 s`; Vite applied 10 files in `76.10 ms` and browser proof in `3.50 s`; static Node applied 10 files in `24.83 ms` and browser proof in `3.11 s`. Both passed test/build, desktop user path, mobile render, API 200/201, zero console/network failures, and persisted four screenshots under `.vnem/giga-evolution/phase-7/browser-evidence`.
- Phase 7 full-suite proof: fixed-clock `npm test` completed naturally with exit code 0. The first full attempt caught a branch-caused six-tool route-budget regression in cross-MCP adoption; the fix narrows generic `repo feature` language out of app-engineering classification and preserves app inspection/planning, repo map/patch targeting, focused tests, GitHub Actions, and proof for their actual material domains. Both adoption regression families and Core GIGA routing tests pass after the fix.
- Phase 8 architecture: `scripts/vnem/tools/project-automation.mjs` owns shell/package-manager/task-runner detection, exact command review, sanitized bounded execution, persistent task graphs, process-tree timeout cleanup, log/port/lock/temp diagnosis, and reversible temp quarantine. Tools exposes eight cohesive calls: `vnem_tools_project_automation_inspect`, `vnem_tools_project_command_run`, task-graph plan/run/status/rollback, `vnem_tools_project_runtime_diagnose`, and `vnem_tools_project_temp_cleanup`.
- Phase 8 command policy: known-safe argv, inspected project-declared scripts, exact reviewed custom commands, and blocked dangerous commands are distinct layers. Custom commands require `run_custom_command`, available only to creator/maintainer/expert or an exact custom grant; temp quarantine requires approved-write capability. Request-provided shell operators, redirection, substitutions, inline shell/code flags, downloaded-code launchers, Git mutation through custom execution, raw secret arguments, unsafe package lifecycle hooks, and stale review ids are blocked. Project-script `pre`/`post` hooks are bound by content hash. Child environments remove secret/auth/credential variables, askpass hooks, `NODE_OPTIONS`, and user npm config before execution.
- Phase 8 task/runtime behavior: task graphs validate acyclic dependency order, persist after every node, recover interrupted `running` nodes, skip satisfied path/hash/port conditions, resume with a node budget, stop on failure by default, and execute only declared rollback commands in reverse order. Long output streams into capped redacted evidence logs and returns compact head/tail summaries. Timeouts record exit/signal state and terminate VNEM-owned Windows trees with `taskkill /T` or exact descendant fallback, and POSIX process groups with TERM/KILL. Runtime diagnosis reads bounded logs first, then ports, known VNEM servers, non-destructive lock signals, temp paths, and interrupted graphs. Unknown processes are never killed.
- Phase 8 reversible cleanup: explicit project-local `.tmp`, `tmp`, `temp`, `.cache`, or `.vnem-temp` paths are inventoried and moved into a collision-safe evidence quarantine with bounded `EBUSY`/`EPERM`/`ENOTEMPTY`/`EACCES` retries. Symlink/junction targets are blocked. A manifest supports exact restore; irreversible deletion is not implemented by this tool.
- Phase 8 real MCP proof: `scripts/test-tools-giga-project-automation.mjs` starts Tools twice through stdio under `safe-local-dev` and `creator-power`. It proves eight registrations, real environment detection, safe-profile custom blocking, known-safe execution, controlled declared `&&` review, dangerous script/lifecycle hook/operator/raw-secret blocking, stale review rejection, sanitized child environment, long-output evidence, exit code 7, a timed-out child tree with no surviving delayed descendant, graph pause/resume/satisfied-skip/rollback, live localhost port/log diagnosis, server stop/port release, and temp quarantine/restore.
- Phase 8 execution benchmark: `.vnem/giga-evolution/phase-8/execution-benchmark.json` preserves 29 real MCP calls completed in `17.58 s`. Windows detected PowerShell, cmd, and bash; a `46,452`-byte output was compacted; the timeout descendant did not survive; graph resume completed and rollback succeeded; the listening port was observed then released; secret environment removal, lifecycle blocking, log-first diagnosis, quarantine, and restore all passed.
- Phase 8 affected checks passing: project-automation MCP test and benchmark outside the managed process sandbox; Tools MCP smoke; legacy project actions; permission profiles/runtime/scoped grants/trust boundary; Core GIGA intelligence/tool selection and both adoption reliability families; Core+Tools ecosystem; Tools/Core readiness; runtime registry/behavior contracts; registry validation; reliability catalog; public-repo hygiene; source control-character guard; `git diff --check`; and link check with external fetch warnings only. Registry totals are 236 (`Core 68`, `Tools 161`, `Precision 7`), with 218 behavior-referenced and the pre-existing 18 Tools behavior-reference warnings unchanged.
- Remaining benchmark gaps: nine scenarios require Tools capabilities intentionally scheduled for later phases: Windows process/port diagnosis, game/mod/Roblox/Luau, API catalog selection, vetted skill selection/execution, client mutation/rollback, and database schema/query tools. Core reports registered fallbacks and explicit capability gaps rather than fake names.
- Unresolved blockers: no Phase 8 implementation blocker. The managed Windows process sandbox denies `taskkill` and CIM process enumeration, so the exact Windows timeout-tree test must run outside that sandbox; it passed there, and the returned evidence reports the denied strategy instead of claiming cleanup when blocked. Lock-owner identification remains partial without an approved OS-specific handle inspector. Standalone command side effects are not automatically reversible; automatic rollback requires task-graph compensating commands. Phase 7 browser limitations, deferred client UI proof, import-only client proof, host performance sampling, and further Phase 2 Core/Tools extraction remain.
- Phase 8 CI correction: Actions run `29259837515` failed only in `test:tools-giga-project-automation` because POSIX stop terminated the `npm` parent without owning or verifying its child listener, then returned success while the port remained occupied. Dev servers now launch in a dedicated POSIX process group; stop sends TERM/KILL to that group, waits for the localhost listener to disappear, and returns `listener_stop_verified`. The exact MCP proof, legacy project-action regression, and regenerated 29-call benchmark pass locally after the fix.
- Remaining mandatory phases: Phase 9 testing/CI/coverage/benchmarking/failure diagnosis; Phase 10 browser/UI interaction; Phase 11 Windows/local-PC tooling; Phase 12 GitHub administration/development; Phase 13 game/modding/Roblox; Phase 14 dependency/security; Phase 15 structural code intelligence; Phase 16 API runtime; Phase 17 skills/plugins; Phase 18 current docs; Phase 19 databases/data; Phase 20 Cloudflare/deployment; Phase 21 deterministic generation; Phase 22 performance/output efficiency; Phase 23 adoption/client-use proof; Phase 24 ARD/dashboard/site/docs and final integration. PR #15 stays draft and must not be merged automatically.
- Exact next action: commit `fix(tools): stop POSIX dev server process groups`, push `feat/vnem-giga-evolution-1`, verify exact remote/PR SHA and fresh GitHub Actions, refresh the draft PR checkpoint, then immediately begin Phase 9 testing/CI/coverage/benchmarking and failure diagnosis.
- Generated artifacts: authoritative runtime registry/docs and behavior contracts were refreshed with the preserved generation clock; `.vnem/giga-evolution/phase-8/execution-benchmark.json` records real volatile execution evidence. Install archives/guides did not churn.
- PR and CI state: draft PR #15 `https://github.com/Ovvuhy/vnem/pull/15` remains open, draft, mergeable, and unmerged. Exact Phase 8 local/remote/PR checkpoint is `7dce21afc1c42c4ae92c8e392be7e06f43da9649`. Actions run `29259837515` failed at the POSIX listener-release assertion described above; fresh CI after the local fix is required. The PR body still records Phases 0-7 and must be refreshed through Phase 8 only after remote green.
